import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    inline: {
        flexDirection: "row",
        alignItems: "center",
    },
    container: {
        flex: 1
    },
    imgSend: {
        width: 30,
        height: 30,
        marginRight: 5
    }
});

export default style;